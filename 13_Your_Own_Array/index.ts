import { log } from "console"

let transport: string[] = ["car", "cycle", "Gmw", "Honda"]
let message: string = "I would like to own a"
for(let i=0; i<transport.length; i++) {
    console.log(message + transport[i]);
    
}