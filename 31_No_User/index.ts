let userName: string[] = ["admin", "alishba", "aiza"];
if(userName.length === 0){
    console.log("We need to fnd more users");
}else{
    userName = [];
    console.log("All users have been removed."+ userName.length);
}